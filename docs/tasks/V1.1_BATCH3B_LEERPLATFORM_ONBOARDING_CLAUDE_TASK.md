# V1.1 Batch 3B — leerplatform onboarding task for Claude Code

## Goal

Onboard `BlazeForce-NL/leerplatform` as a real consumer of the canonical `BlazeForce-NL/ai-capabilities` platform using the already-merged Batch 3A profile `projects/leerplatform.yaml`.

This repository already contains hand-authored AI instructions. The onboarding must therefore prove **no-loss reconciliation** before replacing them with generated runtime projections.

## Source of truth

Use current `main` of `BlazeForce-NL/ai-capabilities` after Batch 3A merge commit `f33fba41fbc2507e08e78554c0a7a9eed1b958ff`.

Read before making changes:

- `projects/leerplatform.yaml`
- `baselines/typescript-app.yaml`
- `baselines/repository.yaml`
- `docs/V1.1_FLEET_ROLLOUT.md`
- `docs/BASELINE_MODEL.md`
- `docs/onboarding/V1.1_BATCH3_SHARED_PLATFORM_INSPECTION.md`
- sync/apply tooling docs and a completed consumer onboarding such as `infra`.

Do not modify canonical profile selection in this consumer PR. If you find the central profile is materially wrong, stop and report the evidence instead of compensating locally.

## Existing AI instructions that must be reconciled

Current `main` contains:

- `AGENTS.md` with a critical Next.js-version warning:
  - this repository's installed Next.js may differ materially from model training data;
  - before writing Next.js code, read the relevant local guide in `node_modules/next/dist/docs/`;
  - heed deprecation notices.
- `CLAUDE.md` containing only `@AGENTS.md`.

This warning is real repository-specific operating context and must survive onboarding. It should live in project-owned instructions, not as a manual edit to generated runtime output.

## Repository-specific context to preserve

The project-local instructions should accurately capture the real repository and only the parts not already covered canonically. At minimum review and preserve, where current repository evidence confirms them:

1. **Next.js local-version rule** — consult the installed local Next.js docs in `node_modules/next/dist/docs/` before changing framework-sensitive code; do not rely blindly on training-memory conventions.
2. **Accessibility / WCAG AA expectations** — keyboard access, appropriate touch targets, accessible UI behavior where the repository's own contributor rules require them.
3. **Child-safety context** — this is a learning platform for primary-school children; avoid patterns that weaken safeguards or expose unnecessary personal data.
4. **Token-first design-system discipline** — preserve the repository's existing rule to use shared design tokens/components rather than ad hoc styling if that rule is present in current docs.
5. **Authentication model** — magic-link / student-login / session behavior should be treated as security-sensitive repository context; do not invent a different auth model.
6. **Verification guard / commit-hook convention** — preserve the repository's existing verification expectations for code-impacting commits if still present and active.

Do not duplicate generic API-design, testing, Git governance or application-security guidance already supplied by the canonical baseline.

## Required implementation

1. Re-inspect current `main` before changing anything.
2. Inventory all current AI-specific/manual instruction files and classify each as:
   - preserve as project-local context,
   - superseded by canonical generated output,
   - unrelated/runtime configuration that should remain.
3. Create/update `.ai/project/repository-instructions.md` with concise, repository-specific context.
4. Perform an explicit no-loss comparison against the old `AGENTS.md` / `CLAUDE.md`.
5. Use the official `ai-capabilities` sync/apply path for project `leerplatform`; do not hand-author generated Claude/Codex/Copilot output.
6. Generated output may replace the old root `AGENTS.md` and Claude routing file only after the important Next.js warning is demonstrably present through the project-owned mirror in generated output.
7. Preserve unrelated repository files exactly.
8. Do not create framework-specific canonical capability content in this consumer PR.

## Validation / Definition of Done

The PR is complete only when all of these are demonstrated:

- central profile `leerplatform` resolves cleanly;
- official sync/apply completes with zero conflicts;
- generated Claude, Codex and Copilot outputs are present and current;
- the old Next.js local-doc warning survives in the generated native instructions through project-local mirroring;
- no manually-maintained duplicate AI routing remains unless clearly justified and outside sync ownership;
- second sync is idempotent: 0 CREATE, 0 UPDATE, 0 DELETE, 0 CONFLICT;
- `--check` exits cleanly;
- repository-native validation passes at minimum:
  - package-manager test command documented by the repo (expected `pnpm test`);
  - package-manager lint command documented by the repo (expected `pnpm lint`);
  - any repository verification-hook/docs check that can be run deterministically in CI/local mode;
- `git diff --check` passes;
- no unrelated changes exist.

If the exact package-manager command differs on current main, use the repository-documented equivalent and explain the deviation.

## Self-review

Before declaring completion, review the diff as if you did not write it. Check specifically for:

- loss of the Next.js local-version warning;
- project-specific guidance copied into canonical/generated sections manually;
- stale framework-version facts hardcoded in project-local instructions;
- duplicated generic security/testing/API guidance already supplied by `typescript-app`;
- weakened child-safety/accessibility rules;
- generated-output drift;
- unrelated formatting or cleanup.

Fix substantiated findings before reporting completion.

## Completion report

Add a PR comment containing:

1. exact manual AI files found on `main` and their dispositions;
2. exact project-local content retained and why;
3. effective baseline/capabilities resolved;
4. first sync/apply result;
5. no-loss evidence for the old `AGENTS.md` warning;
6. second-run/idempotence result;
7. `--check` result;
8. native test/lint/verification results;
9. exact files changed;
10. deviations or unresolved questions.

Do not merge this PR.
