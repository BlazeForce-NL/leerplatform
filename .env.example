# Kopieer dit bestand naar .env.local en vul de waarden in.
# Commit .env.local NOOIT naar git.

# ── Database ──────────────────────────────────────────────────────────────────
# Postgres-verbinding (gebruik Supabase EU, Neon EU of Railway EU voor AVG-compliance)
DATABASE_URL="postgresql://user:password@localhost:5432/leerplatform"

# ── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"
